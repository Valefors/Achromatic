using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Enums 
{
    public enum EMovableType
    {
        NONE,
        ASHTRAY,
        BOTTLE,
        BOOK,
        GLASS
    }

    public enum ERotatingType
    {
        NONE,
        PAPER
    }

    public enum ELanguage
    {
        ENGLISH,
        FRENCH
    }
}
